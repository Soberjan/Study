#include <iostream>
#include <time.h>
#include <bitset>
using namespace std;

int main(int c, char** v){
//    srand(atoi(v[1]) + time(0));
//    for (int i = 0; i < atoi(v[2]); i++)
//        cout << 1 + rand() % 8;
    return 0;
}
