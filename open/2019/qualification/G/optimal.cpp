#include <iostream>
#include <stdio.h>
#include <algorithm>
using namespace std;

int n, m, a[10000];
pair<int, int> v[100000];
int main()
{
    //freopen("tests/03", "r", stdin);
    cin >> n >> m;
    for (int i = 0; i < n; i++)
        cin >> a[i];
    for (int i = 0; i < n; i++)
        cin >> v.first >> v.second;


    return 0;
}
